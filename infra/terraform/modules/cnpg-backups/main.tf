# STUB (Phase 3) — object storage for CloudNativePG PITR backups (barman-cloud plugin).
# Provider implementation creates the bucket + an access policy/role CNPG can assume. No resources yet.
# See ADR 0008 (we own Postgres ops, incl. backups/PITR).
