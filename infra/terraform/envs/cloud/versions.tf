terraform {
  required_version = ">= 1.6"
  # No providers required yet — the cluster/backup modules are stubs (Phase 3). When a provider is
  # chosen, add its required_providers block here and a provider "..." config below.
}
