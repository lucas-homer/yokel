# Generated, vendored, and frozen artifacts — excluded from formatting so the lint gate
# only governs hand-authored source. (Prettier already reads the root .gitignore too.)

# Dependency lockfile (generated)
pnpm-lock.yaml

# Heavy generated research/foundry reports — "do NOT read routinely" (AGENTS.md). Reformatting
# these 100KB+ HTML/JSON/markdown artifacts is pure noise.
docs/research/

# Foundry-generated architecture docs — AGENTS.md: generated from foundry output, "left as-is".
docs/architecture/

# Frozen Week-1 evidence artifacts — generated by the spike scripts; kept verbatim.
spikes/out/

# Terraform provider/module cache (gitignored per-dir; excluded here too for `prettier .`).
**/.terraform/

# Helm chart templates — Go templating ({{ ... }}, {{- range }}) is not valid YAML, so prettier's
# YAML parser errors on them. Chart.yaml / values*.yaml ARE formatted (they're plain YAML).
charts/**/templates/

