# CLOUD overlay — STUB (Phase 3). The env-specific seams that differ from local; shapes stay identical.
postgres:
  instances: 3 # HA
  imageName: "" # TODO(phase-3): pin exact 16.x
  storageClass: "" # TODO(phase-3): cloud CSI class (e.g. gp3 / pd-ssd)
  # PITR to Cloudflare R2 (the backups phase gave phase-3 a concrete provider candidate — R2 is
  # already the offsite mirror target locally, so cutover is endpoint + creds, same shapes).
  backup:
    enabled: true
    endpointURL: "" # TODO(phase-3): https://<account-id>.r2.cloudflarestorage.com
    destinationPath: "" # TODO(phase-3): s3://yokel-backups/cnpg-docketclock/
    creds:
      vaultPath: backups/r2
      accessKeyProperty: access_key_id
      secretKeyProperty: secret_access_key

ingress:
  enabled: true
  className: nginx # TODO(phase-3): cloud ingress + cert-manager TLS
  host: docketclock.example.com

# NOTE: the ClusterSecretStore's cloud auth swap (dev root token → kubernetes auth method, no static token)
# now lives with the hoisted store manifest, not here — see infra/argocd/manifests/vault-backend/
# clustersecretstore.yaml (#51). The Vault *server* seal swap (transit → gcpckms/awskms, replicas → 3, drop
# vault-transit) lives in the cloud platform-vault Application — see platform-vault.yaml comments.
