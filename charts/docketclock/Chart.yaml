apiVersion: v2
name: docketclock
description: The DocketClock substrate — Fastify API + workers, a CloudNativePG Postgres cluster, and
  External-Secrets wiring. Deployed by Argo CD from git; env via values-local.yaml / values-cloud.yaml.
type: application
version: 0.1.0
appVersion: "0.0.0"
