interface:
  display_name: "DocketClock Review"
  short_description: "Review deadline evidence and record queue verdicts"
  default_prompt: "Use $docketclock-review to inspect the live queue and help me work through the highest-priority items one at a time."
